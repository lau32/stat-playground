import { DashboardFacade } from './dashboard.facade';

describe('DashboardFacade', () => {
  beforeEach(() => {
  });

  describe('used in NgModule', () => {
    beforeEach(() => {
    });

    it('', () => {

    })
  });
});
